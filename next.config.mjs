/** @type {import('next').NextConfig} */

// Deployed on Vercel, which builds and serves the Next.js app directly — no
// static export, basePath, or asset prefix needed.
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
